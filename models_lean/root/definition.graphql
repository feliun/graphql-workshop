type Query {
  character(name: String!): Character
}

type Mutation {
  addCharacter(input: InputCharacter!): Character
  updateCharacter(input: InputCharacter!): Character
  deleteCharacter(input: InputCharacter!): Character
}
